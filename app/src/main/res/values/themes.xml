<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BookAppWithFirebase" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.BookAppWithFirebase" parent="Base.Theme.BookAppWithFirebase" />

    <style name="MyTextStyle" parent="@android:style/TextAppearance">
        <item name="android:fontFamily">@font/caladea</item>
    </style>
</resources>